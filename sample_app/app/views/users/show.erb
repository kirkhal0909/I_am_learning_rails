<div class="row">

        <section class="user_info">
            <h1>
                <%= gravatar_for @user%>
                <%= "#{@user.name} <#{@user.email}>" %>
            </h1>
        </section>

</div>
